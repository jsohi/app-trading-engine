{
  "effortLevel": "high",
  "permissions": {
    "ask": [
      "Bash(gh pr merge:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "BEFORE exiting plan mode, you MUST complete a full plan review loop. This is automatic — do NOT skip it.\n\n1. Spawn 2 review agents IN PARALLEL on the ENTIRE plan file (not just recent changes):\n   - Agent A (Industry Design Checker): Read the full plan. Cross-check every design decision against CLAUDE.md conventions: zero-alloc hot path, Agrona collections (no java.util.*), fixed-point pricing (long x 10^-8), deterministic cluster (no wall-clock, no randomness), SBE/FIX field IDs, final var for reference types, explicit final for primitives, threading model on every class, Javadoc standards, clock discipline (injected EpochNanoClock/NanoClock), logging (GFLog hot-path, Log4j2 infra, no SLF4J). Flag anything that contradicts or omits these conventions.\n   - Agent B (Completeness + Quality): Check for: missing edge cases, missing error handling, missing test plan, missing verification steps, vague language (should/might/consider), ambiguous instructions, incomplete module coverage, no TODO/TBD without Linear issue reference.\n\n2. Fix ALL findings in the plan file. Nothing accepted, nothing out of scope (dev phase).\n\n3. Re-run BOTH agents on the ENTIRE plan (not just changes). Loop until zero findings.\n\n4. Print the plan compliance report (MEASURED, never guessed):\n   Convention Alignment | Completeness | Testability | Error Handling | Threading Model | Allocation Model | Specificity\n   Each scored PASS/SUBPAR/FAIL with actual finding counts.\n   Accepted/Out-of-scope table (MUST be empty).\n\n5. ONLY THEN proceed with ExitPlanMode.",
            "timeout": 15,
            "statusMessage": "Enforcing plan review gate..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/hooks/pre-tool-use.sh",
            "timeout": 5,
            "statusMessage": "Checking Linear issue convention..."
          },
          {
            "type": "command",
            "command": ".claude/hooks/enforce-review-before-push.sh",
            "timeout": 5,
            "statusMessage": "Checking review + E2E gate..."
          },
          {
            "type": "prompt",
            "prompt": "Check if this Bash command is: (1) a git push to main — if it pushes directly to main (e.g. 'git push origin main', or 'git push' while on main branch), BLOCK it with 'No direct pushes to main — all changes must go via PRs'. (2) a git commit — verify the FULL command text contains 'APP-{N}: ' where N is a number (Linear issue reference) — note the commit message may span multiple lines in a heredoc. (3) creates a new branch (git checkout -b, git branch <name>, git switch -c) — verify the branch name matches 'feat/app-{N}-*' pattern. If the command is not related to git push/commit/branch creation, ALLOW it. If it violates a convention, BLOCK it with an explanation. Command: $ARGUMENTS",
            "timeout": 10,
            "statusMessage": "Validating Linear issue convention (LLM)..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pr=$(gh pr view --json number -q .number 2>/dev/null); [ -n \"$pr\" ] && gh pr comment \"$pr\" --body \"@gemini review\" >/dev/null 2>&1; exit 0",
            "if": "Bash(git push:*)",
            "async": true,
            "timeout": 30,
            "statusMessage": "Requesting Gemini review on PR..."
          },
          {
            "type": "command",
            "command": "[ \"$EXIT_CODE\" -eq 0 ] && jq -nc '{decision:\"block\", reason:\"A git commit just completed. You MUST now run ALL three test suites before pushing or doing anything else:\\n\\n  1. ./gradlew test\\n  2. ./gradlew :integration-tests:test\\n  3. ./gradlew e2e\\n\\nDo NOT skip any suite. Do NOT claim they were already run. Execute each command and report results.\"}' || exit 0",
            "if": "Bash(git commit:*)",
            "timeout": 5,
            "statusMessage": "Enforcing post-commit test gate..."
          }
        ]
      }
    ]
  }
}
